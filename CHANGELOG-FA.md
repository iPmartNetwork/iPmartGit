<div dir="rtl">

# تاریخچه تغییرات

تمام تغییرات قابل توجه iPmartGit در این فایل مستند می‌شوند.

---

## [1.0.0] - ۲۰۲۴-۱۲-۰۱

### 🎉 انتشار اولیه

---

#### قابلیت‌های اصلی
- **مدیریت ریپازیتوری** — ایجاد، حذف، Fork ریپازیتوری‌های عمومی و خصوصی
- **مدیریت فایل** — آپلود (کشیدن و رها کردن)، ویرایش آنلاین، حذف، ایجاد فایل جدید
- **میرور از GitHub** — میرور کردن ریپازیتوری‌ها با قابلیت سینک مجدد
- **دانلود ZIP** — دانلود هر ریپازیتوری به صورت آرشیو ZIP
- **پروتکل Git HTTP** — پشتیبانی از `git clone` و `git push` واقعی

---

#### همکاری تیمی
- **Pull Request** — ایجاد PR بین fork ها، بررسی، merge، بستن
- **Code Review** — کامنت خط به خط روی فایل‌ها در Pull Request
- **Issues** — سیستم کامل ردیابی مشکلات با کامنت و وضعیت باز/بسته
- **برچسب‌ها و Milestone** — سازماندهی Issues با برچسب و نقاط عطف
- **همکاران** — اضافه کردن اعضای تیم با دسترسی خواندن/نوشتن/ادمین
- **سازمان‌ها** — ایجاد تیم‌ها با ریپازیتوری‌های مشترک و نقش‌های عضویت

---

#### پلتفرم
- **احراز هویت** — ثبت‌نام، ورود، خروج، مدیریت Session
- **مدیریت پروفایل** — ویرایش نام نمایشی، ایمیل، بیو، تغییر نام کاربری و رمز عبور
- **توکن‌های API** — توکن‌های دسترسی شخصی برای Git HTTP و API
- **Releases** — انتشار نسخه‌ها با نام تگ، تاریخچه تغییرات و فایل‌های پیوست
- **اعلان‌ها** — اعلان لحظه‌ای برای ستاره، Issue، PR، کامنت
- **فید فعالیت** — ردیابی فعالیت‌ها در ریپازیتوری‌ها
- **جستجو** — جستجوی ریپازیتوری‌ها و جستجو در محتوای فایل‌ها
- **تاریخچه فایل** — ردیابی تغییرات فایل با نمایش Diff بصری
- **Import/Export** — پشتیبان‌گیری و بازیابی کامل ریپازیتوری به صورت JSON

---

#### تجربه کاربری
- **Syntax Highlighting** — بیش از ۳۰ زبان برنامه‌نویسی با highlight.js
- **تم تاریک/روشن** — تغییر بین تم‌ها، ذخیره در localStorage
- **میانبرهای کیبورد** — ناوبری سریع (کلید `?` برای مشاهده همه)
- **طراحی ریسپانسیو** — سازگار با موبایل و دسکتاپ
- **رابط فارسی RTL** — پشتیبانی کامل راست‌به‌چپ با فونت وزیرمتن
- **Breadcrumb** — ناوبری آسان مسیر فایل
- **پوشه‌های قابل باز/بسته** — کلیک برای نمایش/مخفی کردن محتوای پوشه

---

#### مدیریت
- **پنل ادمین** — مدیریت تمام کاربران و ریپازیتوری‌ها، مشاهده آمار سیستم
- **مدیریت کاربران** — ارتقا/تنزل ادمین، حذف کاربران
- **آمار سیستم** — تعداد کاربران، ریپازیتوری‌ها، فایل‌ها، حجم ذخیره‌سازی

---

#### استقرار
- **پشتیبانی Docker** — فایل‌های Dockerfile و docker-compose.yml
- **پیکربندی Nginx** — تنظیمات آماده reverse proxy
- **استقرار خودکار** — اسکریپت Bash برای نصب یک‌دستوری روی سرور
- **سرویس Systemd** — اجرای خودکار بعد از ریبوت سرور
- **دیتابیس SQLite** — بدون پیکربندی، بدون نیاز به دیتابیس خارجی

---

#### امنیت
- **هش رمز عبور** — bcrypt با salt rounds
- **مدیریت Session** — کوکی‌های HTTP-only امن، انقضای ۷ روزه
- **محدودیت نرخ** — Rate Limiting روی API برای جلوگیری از سوءاستفاده
- **کنترل دسترسی** — ریپازیتوری خصوصی، دسترسی همکاران، نقش‌های ادمین
- **اعتبارسنجی ورودی** — اعتبارسنجی سمت سرور روی تمام ورودی‌ها
- **اطلاعات ادمین** — نام کاربری و رمز عبور هر دو از صفحه تنظیمات قابل تغییر هستند

---

## نقشه راه

### برنامه‌ریزی شده برای نسخه 1.1.0
- [ ] پشتیبانی Webhook (ارسال event به URL خارجی)
- [ ] احراز هویت دو مرحله‌ای (2FA)
- [ ] اعلان‌های ایمیلی
- [ ] موضوعات/تگ‌های ریپازیتوری
- [ ] ریپازیتوری‌های پین شده در پروفایل

### برنامه‌ریزی شده برای نسخه 1.2.0
- [ ] صفحات Wiki برای ریپازیتوری‌ها
- [ ] بوردهای پروژه (Kanban)
- [ ] قالب‌های ریپازیتوری
- [ ] تأیید امضای Commit
- [ ] احراز هویت کلید SSH برای Git

### برنامه‌ریزی شده برای نسخه 2.0.0
- [ ] بک‌اند Git واقعی (جایگزینی ذخیره‌سازی فایل‌محور)
- [ ] مدیریت Branch
- [ ] حل تعارض Merge
- [ ] اجراکننده CI/CD
- [ ] سیستم پلاگین

</div>
